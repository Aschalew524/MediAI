/** Session cookie + localStorage key (must match `middleware.ts`). */
export const ACCESS_TOKEN_KEY = "mediai_access_token";

/** localStorage key for the opaque refresh token. Never stored in a cookie. */
export const REFRESH_TOKEN_KEY = "mediai_refresh_token";
