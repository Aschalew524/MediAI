# NestJS API (global prefix /api)
NEXT_PUBLIC_API_URL=http://localhost:4000/api

# Google sign-in button: set to "false" to hide (Nest still needs GOOGLE_* for OAuth to work).
# NEXT_PUBLIC_GOOGLE_SIGNIN=false

# Optional: show “RAG on” in chat UI when using the Next.js /api/chat/config stub (Nest GET /api/chat/config returns ragEnabled from RAG_ENABLED)
NEXT_PUBLIC_RAG_ENABLED=1
